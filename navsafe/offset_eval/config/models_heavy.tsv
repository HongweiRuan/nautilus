# Models that cannot share a card with the renderer, run as their own two-card
# fleet over all 54 scenarios.
#
#   simwam_base      SimWAM before FlowGRPO. 12.6 GiB measured for SimWAM-RL,
#                    which is the same architecture and the same checkpoint size.
#   mtdrive_sft      Qwen2.5-VL-7B text-out planner, ~15 GiB in bf16 (16 GB on
#                    disk). The SFT stage.
#   mtdrive_mtgrpo   the same after mtGRPO -- the before/after pair.
#
# simwam's two env vars are paths the adapter would otherwise take from
# constants under /bigdata, which this cluster does not mount: the repo, and the
# Wan2.2 VAE + umT5 base weights DiffSynth loads. mtdrive needs none -- its
# checkpoint is a stage DIRECTORY and it reads nothing from the environment.
simwam_base	simwam	simwam/weights/SimWAM.pt	NAVSAFE_SIMWAM_REPO=/avl-west/navsafe_eval/vla_repos/SimWAM,DIFFSYNTH_MODEL_BASE_PATH=/avl-west/navsafe_eval/model_zoo/_base/diffsynth
mtdrive_sft	mtdrive	mtdrive/mtdrive_sft
mtdrive_mtgrpo	mtdrive	mtdrive/mtdrive_rl_best
