# STAGE drivearena: WorldDreamer generates CAM_F0 for a SHARD of scenarios.
#
# Sharding is BY SCENARIO, never within one: generation is autoregressive, each frame
# conditioned on the previous generated frame, and `is_first_frame` re-anchors on the
# real image at a scenario boundary. Splitting a scenario across jobs would break that
# chain and change the output.
#
# Runs the same offline entry point upstream documents (tools/test.py +
# test_config_nup+nus + the released nus_nup checkpoint), with four upstream bugs
# patched -- see /avl-west/drivearena_bench/README.md. Most important of those: the
# demo caption hard-coded in test.py would otherwise prompt every scenario as
# "boston-seaport, night".
#
# GPU job. Prereq: infos stage. Placeholders: sh003-0828004624 + a manifest block of sids
apiVersion: batch/v1
kind: Job
metadata:
  {
    name: rqe-da-sh003-0828004624,
    namespace: cogrob,
    labels: { app: render_quality_eval, stage: drivearena, shard: "sh003-0828004624" },
  }
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 7200
  template:
    metadata: { labels: { app: render_quality_eval, stage: drivearena, shard: "sh003-0828004624" } }
    spec:
      restartPolicy: Never
      containers:
        - name: worlddreamer
          image: robinwangucsd/metabench:latest
          command: ["bash", "-c"]
          args:
            - |
              # NOT `set -u`: conda's binutils activate.d script dereferences
              # ADDR2LINE unset, so nounset kills the shell on `conda activate`.
              set -o pipefail
              cat > /tmp/shard.txt <<'MANIFEST_EOF'
              089e3eac4f7e5c5c
              08dfae1bbef35bdc
              096d823b664e5972
              09bfb9a8f83b5079
              09d33cf8b87c5ae3
              0a51eb8adf8e5391
              0af464147919575d
              0b1250020ea95539
              0b12ac5676875ade
              0b1a626ef31150b3
              0b1af3d560f5570d
              0ba54149d1575f95
              0ba595e7e63d5a30
              0bc64fd1b3ff5d1a
              0bcae698fd905226
              0c49c66ca32551c2
              0ce170237acd51c2
              0d07429bb7205688
              0d2403a8031f540c
              MANIFEST_EOF
              echo "[drivearena sh003-0828004624] $(grep -c . /tmp/shard.txt) scenario(s)"
              # the pod image exports a global PYTHONPATH that drags a torch-2.10
              # `nexussim` into every interpreter
              unset PYTHONPATH
              ROOT=/avl-west/drivearena_bench
              RQE=/avl-west/render_quality_eval
              WD=$ROOT/DriveArena/WorldDreamer
              source /opt/conda/etc/profile.d/conda.sh
              conda activate $ROOT/envs/dreamer
              cd $WD
              # per-shard infos + token map, so shards never write the same file
              export WD_RENDER_ROOT=$RQE/render_raw/drivearena
              export WD_TOKEN_MAP=/tmp/shard_tokens.json
              export WD_FRONT_ONLY=1
              mkdir -p "$WD_RENDER_ROOT"
              # reads WD_RENDER_ROOT and drops scenarios already fully generated
              python3 $RQE/make_shard_infos.py /tmp/shard.txt /tmp/shard_infos.pkl /tmp/shard_tokens.json \
                | tee /tmp/shard_infos.log || { echo "SHARD_INFOS_FAILED"; exit 1; }
              if grep -q SHARD_ALREADY_COMPLETE /tmp/shard_infos.log; then
                echo "SHARD_DONE sh003-0828004624 (nothing left to generate)"; exit 0
              fi
              python tools/test.py \
                --config-name test_config_nup+nus \
                resume_from_checkpoint=$WD/dreamer_pretrained/SDv1.5_mv_single_ref_nus_nup/weight-S200000 \
                runner.validation_index=all \
                runner.enable_xformers_memory_efficient_attention=false \
                '~dataset.data.val.nuscenes' \
                dataset.dataset_root=$ROOT/nuplan_root \
                dataset.data.val.nuplan.ann_file=/tmp/shard_infos.pkl \
                'dataset.dataset_cache_file=[null,null]' \
                show_box_on_img=false show_map_on_img=false
              # Verify THIS shard's own expected outputs. `find` over the shared
              # render root counts every shard's frames, so it can report a healthy
              # number while this shard produced nothing -- which is exactly how a
              # run where every reference image was unreadable still exited 0.
              missing=0
              while read -r rel; do
                [ -z "$rel" ] && continue
                [ -f "$WD_RENDER_ROOT/$rel" ] || missing=$((missing+1))
              done < /tmp/shard_tokens.json.expected
              want=$(grep -c . /tmp/shard_tokens.json.expected)
              echo "SHARD_DONE sh003-0828004624 generated=$((want-missing))/$want missing=$missing"
              [ "$missing" -eq 0 ] || exit 1
          resources:
            # Right-sized from `kubectl top` on 8 running pods: 1.1-1.6 CPU,
            # 12-13Gi RAM. These nodes are 28 CPU / 8 GPU, so at the old cpu:8
            # a single pod reserved the CPU share of 2.3 GPUs and only 8 of 20
            # shards could schedule -- with all 48 GPUs sitting idle.
            requests: { cpu: "3", memory: "20Gi", nvidia.com/gpu: "1", ephemeral-storage: 60Gi }
            limits:   { cpu: "3", memory: "20Gi", nvidia.com/gpu: "1", ephemeral-storage: 60Gi }
          volumeMounts:
            - { name: dshm, mountPath: /dev/shm }
            - { name: avl-west-vol, mountPath: /avl-west }
      volumes:
        - { name: dshm, emptyDir: { medium: Memory, sizeLimit: 16Gi } }
        - { name: avl-west-vol, persistentVolumeClaim: { claimName: avl-west-vol } }
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - { key: topology.kubernetes.io/region, operator: In, values: [us-west] }
                  - { key: nvidia.com/gpu.product, operator: In, values: [NVIDIA-GeForce-RTX-3090] }
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - ry-gpu-05.sdsc.optiputer.net
                      - ry-gpu-06.sdsc.optiputer.net
                      - ry-gpu-07.sdsc.optiputer.net
                      - ry-gpu-08.sdsc.optiputer.net
                      - ry-gpu-11.sdsc.optiputer.net
                      - ry-gpu-12.sdsc.optiputer.net
      tolerations:
        - { effect: NoSchedule, key: nautilus.io/reservation, operator: Equal, value: cogrob }
